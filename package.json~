{
  "name": "fabricate-ui-regression-tests",
  "version": "1.0.0",
  "description": "starship-fabricate-ui-regression tests for starship-fabricate-ui project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "skapoor",
  "license": "ISC"
}
